{
  "name": "@cardano-foundation/cardano-connect-with-wallet",
  "private": true,
  "license": "Apache-2.0",
  "description": "This package aims to provide useful hooks and React components to simplify the cardano dapp integration",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn build:react",
    "build:react": "yarn workspace @cardano-foundation/cardano-connect-with-wallet-react run build-storybook",
    "test": "yarn test:react",
    "test:react": "yarn workspace @cardano-foundation/cardano-connect-with-wallet-react run test"
  },
  "browserslist": [
    "defaults"
  ],
  "devDependencies": {
    "lerna": "^7.1.1"
  }
}

